my-nuxt-app/
│── .nuxt/ # Auto-generated by Nuxt (don’t edit)
│── node_modules/ # Dependencies
│── public/ # Static files (robots.txt, favicon.ico, images, etc.)
│── .env # Environment variables
│── nuxt.config.ts # Nuxt config
│── package.json
│── tsconfig.json
│
├── assets/ # Uncompiled assets (CSS, SCSS, fonts, images)
│ └── css/
│ └── main.css # Global styles
│
├── components/ # Vue components (buttons, modals, headers, etc.)
│ └── ui/ # Grouped by feature (e.g. reusable UI components)
│
├── composables/ # Reusable logic (like Vue hooks)
│ └── useAuth.ts # Example: authentication helper
│
├── layouts/ # App layouts
│ └── default.vue # Default layout (navbar, footer, etc.)
│ └── admin.vue # Admin dashboard layout
│
├── middleware/ # Route middleware (auth guards, logging, etc.)
│ └── auth.global.ts # Example: redirect if not logged in
│
├── pages/ # Auto-routed pages
│ └── index.vue # Home page
│ └── login.vue # Login page
│ └── admin/ # Nested routes
│ └── index.vue # /admin
│ └── users.vue # /admin/users
│
├── plugins/ # Nuxt plugins
│ └── pinia.ts # Pinia setup
│ └── api.ts # Example: API client (axios, fetch wrapper)
│
├── server/ # Server routes (backend API with Nitro)
│ └── api/
│ └── auth.ts # Example: /api/auth endpoint
│
├── stores/ # Pinia stores
│ └── user.ts # User store
│ └── counter.ts # Example counter
│
└── utils/ # Helper functions
└── validators.ts # Example: form validators
